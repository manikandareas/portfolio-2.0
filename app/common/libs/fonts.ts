import { Fira_Code, Inter, Plus_Jakarta_Sans, Sora } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });

export const firaCode = Fira_Code({ subsets: ["latin"] });

export const jakartaSans = Plus_Jakarta_Sans({ subsets: ["latin"] });

export const soraSans = Sora({ subsets: ["latin"] });
